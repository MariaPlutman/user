[user]
	name = MariaPlutman
	email = mariaplutman@gmail.com
[color]
	ui = true
[core]
	editor = emacs
[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
